---
spring:
  cloud:
    config:
      server:
        git:
          uri: ${vcap.services.srvroute_config.credentials.uri}
          # ONLY include username and pwd if the repo is private
          username: ${vcap.services.srvroute_config.credentials.username}
          password: ${vcap.services.srvroute_config.credentials.password}
  security:
    user:
      name: admin
      password: admin
info:
  description: Training configuration repo
